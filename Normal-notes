 pg_dump -U postgres -h 192.168.10.13 -p 5432 -d Plant > plant.sql
Password: 
 
 
 
 2000  sudo rm -rf /usr/local/google-cloud-sdk /opt/google-cloud-sdk ~/google-cloud-sdk
 2001  sudo rm -f /etc/apt/trusted.gpg.d/google-cloud.gpg
 2002  sudo rm -f /usr/share/keyrings/cloud.google.gpg
 2003  sudo rm -f /etc/apt/sources.list.d/google-cloud-sdk.list
 2004  clear
 2005  ls
 2006  sudo apt-get update
 2007  sudo apt-get install apt-transport-https ca-certificates gnupg
 2008  curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo gpg --dearmor -o /usr/share/keyrings/cloud.google.gpg
 2009  echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] https://packages.cloud.google.com/apt cloud-sdk main" | sudo tee /etc/apt/sources.list.d/google-cloud-sdk.list
 2010  sudo apt-get update
 2011  sudo apt-get install google-cloud-cli
 2012  gcloud init
 2013  gcloud container clusters create lab-cluster --zone us-west1-b --num-nodes 5
 gcloud container clusters create lab-clustering --zone us-west4-a --num-nodes 10 --machine-type n1-standard-4
 gcloud container clusters create lab-cluster --zone us-west4-c --num-nodes 4 --machine-type e2-highmem-4 --disk-size=30
 2014  kubectl get nodes
 2015  nano ~/.kube/config
 2016  kubectl get nodes
 2017  kubectl get pods
 2018  ls 
 2019  cd herbal-garden-application/
 2020  ls
 2021  helm list
 2022  helm install herbal-final .
 2023  kubectl get pods
 2024  kubectl get svc
 2025* helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
 2026  kubec
 2027  kubectl get svc
 2028  helm upgrade herbal-final .
 2029  history
#=======================================

 #  helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
# helm repo update
# helm install ingress-nginx ingress-nginx/ingress-nginx   --namespace ingress-nginx   --create-namespace   --set controller.service.type=LoadBalancer


#================================== grafana & prometheus ===========================
#docker run --rm -it -v virtual-herbal-garden_grafana-storage:/data alpine sh

#=========== helm grafana and prometheus============================================
# helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
# helm repo update
# helm install prometheus-stack prometheus-community/kube-prometheus-stack --namespace monitoring --create-namespace
# from the above command both grafana and prometheus will be installed

# helm show values prometheus-community/kube-prometheus-stack

# helm install monitoring prometheus-community/kube-prometheus-stack \
# -n monitoring \
# -f ./custom_kube_prometheus_stack.yml


# #==========
# NOTES:
# kube-prometheus-stack has been installed. Check its status by running:
#   kubectl --namespace monitoring get pods -l "release=prometheus-stack"

# Get Grafana 'admin' user password by running:

#   kubectl --namespace monitoring get secrets prometheus-stack-grafana -o jsonpath="{.data.admin-password}" | base64 -d ; echo
#(prom-operator)

# Access Grafana local instance:

#   export POD_NAME=$(kubectl --namespace monitoring get pod -l "app.kubernetes.io/name=grafana,app.kubernetes.io/instance=prometheus-stack" -oname)
#   kubectl --namespace monitoring port-forward $POD_NAME 3000

# Visit https://github.com/prometheus-operator/kube-prometheus for instructions on how to create & configure Alertmanager and Prometheus instances using the Operator.


#  1950  kubectl port-forward svc/prometheus-stack-kube-prometheus-prometheus 9090:9090 -n monitoring
#  1951  kubectl get services -n monitoring
#  1952  kubectl port-forward svc/prometheus-stack-kube-prom-prometheus 9090:9090 -n monitoring



# update the ip address for the prometheus 
#(base) flgpu@flgpu-HP-Z420-Workstation:/mnt/data1Tb/workspace/dharshan/virtual-herbal-garden$ sudo nano /etc/hosts

#================== ==================================================\
#http://prometheus-stack-kube-prom-prometheus.monitoring.svc.cluster.local:9090 the url used in grafana to add the source

sum((rate(http_requests_total{job="herbal-garden-backend-service", status="5xx"}[5m])))
histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))
sum(rate(http_requests_total{job="herbal-garden-backend-service", handler="/plants/"}[5m]))
sum(rate(node_cpu_seconds_total{mode="idle"}[5m]))
sum(node_memory_MemAvailable_bytes)


# #==================================== Logging =====================================#
# # add the elastic and install eastic search and kibana  repo to the system through helm
# helm repo add elastic https://helm.elastic.co
# helm repo update

# helm install elasticsearch elastic/elasticsearch --namespace logging --create-namespace --set replicas=1
# helm install elasticsearch elastic/elasticsearch --namespace logging --set replicas=1 --set resources.requests.memory=1024Mi --set persistence.enabled=false

# helm install kibana elastic/kibana --namespace logging --set service.type=LoadBalancer


# helm install elasticsearch elastic/elasticsearch --namespace logging --set replicas=1 --set esJavaOpts="-Xms1g -Xmx1g" --set service.type=ClusterIP --set service.port=9200
# NAME: elasticsearch
# LAST DEPLOYED: Fri Sep 12 11:02:35 2025
# NAMESPACE: logging
# STATUS: deployed
# REVISION: 1
# NOTES:
# 1. Watch all cluster members come up.
#   $ kubectl get pods --namespace=logging -l app=elasticsearch-master -w
# 2. Retrieve elastic user's password.
#   $ kubectl get secrets --namespace=logging elasticsearch-master-credentials -ojsonpath='{.data.password}' | base64 -d
# # get the secret and store it and use it in kibana-values.yaml file
# 3. Test cluster health using Helm test.
#   $ helm --namespace=logging test elasticsearch


# /mnt/data1Tb/workspace/dharshan/virtual-herbal-garden/herbal-garden-application$ helm install kibana1 elastic/kibana --namespace logging -f templates/kibana-values.yaml





# #==================================================================================#helm repo add bitnami https://charts.bitnami.com/bitnami
# helm repo add bitnami https://charts.bitnami.com/bitnami
# helm repo update
# helm install elasticsearch bitnami/elasticsearch --namespace logging --create-namespace
# helm install kibana bitnami/kibana --namespace logging
# helm install logstash bitnami/logstash --namespace logging

# #==================================================================================#
# #=============================== Jaeger tracing ===================================#
# helm repo add jaegertracing https://jaegertracing.github.io/helm-charts
# helm repo update
# helm install jaeger jaegertracing/jaeger --namespace tracing --create-namespace




#======================================= New relic ===================================#
helm repo add newrelic https://helm-charts.newrelic.com
flgpu@flgpu-HP-Z420-Workstation:/mnt/data1Tb/workspace/dharshan/virtual-herbal-garden/herbal-garden-application/templates$ helm install newrelic-release newrelic/nri-bundle -f newrelicvalues.yaml --namespace newrelic --create-namespace






KSM_IMAGE_VERSION="v2.13.0" && 
helm repo add newrelic https://helm-charts.newrelic.com && helm repo update && kubectl create namespace newrelic ; 
helm upgrade --install newrelic-bundle newrelic/nri-bundle --set global.licenseKey=da2c2e55c8b5bee20bf9e98b055dcd86FFFFNRAL --set global.cluster=lab-cluster --namespace=newrelic --set global.lowDataMode=true --set kube-state-metrics.image.tag=${KSM_IMAGE_VERSION} --set kube-state-metrics.enabled=true --set kubeEvents.enabled=true --set newrelic-prometheus-agent.enabled=true --set newrelic-prometheus-agent.lowDataMode=true --set newrelic-prometheus-agent.config.kubernetes.integrations_filter.enabled=false --set k8s-agents-operator.enabled=true --set logging.enabled=true --set newrelic-logging.lowDataMode=true
helm repo update ; helm upgrade --install newrelic-bundle newrelic/nri-bundle -n newrelic --values newrelicvalues.yaml



